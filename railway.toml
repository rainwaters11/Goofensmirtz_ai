[project]
name = "pet-pov-ai"

[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter './packages/**' build && pnpm --filter @pet-pov/worker build"

[deploy]
startCommand = "pnpm --filter @pet-pov/worker start"
